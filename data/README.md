#### Drop the ForestRisks data folder from Zenodo in here, and unzip everything.

Remember to `gunzip` the `.gz` NetCDFs!

If you don't move anything else around, all the paths in the code should work.
